Run S3, Dynamo DB, MySql and Web-Server:
--
1) Run s3 and dynamo DB for store data and blocks async calls
2) Run MySql and store its data to S3
3) Run WebServer cluster and Get data about MySql from S3

Commands
--
1) terraform init
2) terraform plan
3) terraform apply
4) terraform destroy